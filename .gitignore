# Build artifacts
result
result-*

# Secrets (NEVER commit these)
*.key
*.pem
*_rsa
*_ed25519
*.gpg
*secrets*
*_secrets*
secrets/
# …but secretspec.toml declares WHICH secrets exist, never a value —
# it is meant to be committed, and `*secrets*` above would eat it.
!secretspec.toml
.env
.envrc

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Backup files
*.backup
*.old
